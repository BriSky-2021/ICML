# Paper: Bridge Maintenance Benchmark
# Python 3.8. Install PyTorch separately first (see readme Installation section).
# For exact tested versions, see requirements-frozen.txt.

# PyTorch 2.4.1+cu121 — install separately, e.g.:
#   pip install torch==2.4.1+cu121 --index-url https://download.pytorch.org/whl/cu121

numpy>=1.20,<2
pandas>=1.3
PyYAML>=5.4
matplotlib>=3.4
seaborn>=0.11
scipy>=1.7
tqdm>=4.60
openpyxl>=3.0
requests>=2.26
